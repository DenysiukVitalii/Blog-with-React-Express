import React, { Component } from 'react';
import './Header.sass';
import './grid.scss';

class Header extends Component {
  render() {
    return (
      <header>
      </header>
    );
  }
}

export default Header;
